function insertChord() {
    const chord = document.getElementById("chord").value;

    // This is a placeholder for the actual Word interaction
    // Later, we'll need to implement the logic to insert the chord above the selected text
    console.log(`Chord selected: ${chord}`);
}
